# Override Raspberry Pi OS default (/usr/lib/.../40-rpi-volatile-storage.conf),
# which sets Storage=volatile (logs in RAM, wiped every boot).
# Persistent logs are needed to diagnose intermittent boot failures on this arcade box.
[Journal]
Storage=persistent
SystemMaxUse=200M
